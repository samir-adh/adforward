from setuptools import setup, find_packages

setup(
    name="adforward",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Samir Abdallah",
    description="Forward-mode automatic differentiation library",
    python_requires=">=3.7",
)
